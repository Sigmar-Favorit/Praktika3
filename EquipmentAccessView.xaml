<Page x:Class="Praktika3.EquipmentAccessView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Доступ к оборудованию" 
      Background="White"
      FontFamily="Bahnschrift Light SemiCondensed">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Доступ к оборудованию" FontSize="22" FontWeight="Bold" Foreground="#0C4882" Margin="0,0,0,20"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <Button x:Name="BtnAdd" Content="Добавить доступ" Width="120" Height="35" Margin="10,0" Background="#0C4882" Foreground="White" BorderThickness="0" Click="BtnAdd_Click"/>
        </StackPanel>
        <DataGrid x:Name="EquipmentGrid" Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True" AlternatingRowBackground="#BBDCFA">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Сотрудник" Binding="{Binding Employee.FullName}" Width="200"/>
                <DataGridTextColumn Header="Оборудование" Binding="{Binding EquipmentName}" Width="200"/>
                <DataGridTextColumn Header="Дата выдачи" Binding="{Binding GrantedDate, StringFormat=dd.MM.yyyy}" Width="120"/>
                <DataGridTextColumn Header="Мастер" Binding="{Binding MasterName}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>