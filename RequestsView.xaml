<Page x:Class="Praktika3.RequestsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Заявки" 
      Background="White"
      FontFamily="Bahnschrift Light SemiCondensed">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Заявки" FontSize="22" FontWeight="Bold" Foreground="#0C4882" Margin="0,0,0,20"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <Button x:Name="BtnRefresh" Content="Обновить" Width="100" Height="35" Background="#0C4882" Foreground="White" BorderThickness="0" Click="BtnRefresh_Click"/>
            <Button x:Name="BtnCreate" Content="Новая заявка" Width="100" Height="35" Margin="10,0" Background="#0C4882" Foreground="White" BorderThickness="0" Click="BtnCreate_Click"/>
            <ComboBox x:Name="StatusFilter" Width="150" Height="35" SelectionChanged="StatusFilter_SelectionChanged">
                <ComboBoxItem Content="Все" IsSelected="True"/>
                <ComboBoxItem Content="Создана"/>
                <ComboBoxItem Content="Предоплата"/>
                <ComboBoxItem Content="Производство"/>
                <ComboBoxItem Content="Доставка"/>
                <ComboBoxItem Content="Выполнена"/>
                <ComboBoxItem Content="Отменена"/>
            </ComboBox>
        </StackPanel>
        <DataGrid x:Name="RequestsGrid" Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True" 
                  AlternatingRowBackground="#BBDCFA" SelectionChanged="RequestsGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="№" Binding="{Binding Id}" Width="60"/>
                <DataGridTextColumn Header="Партнер" Binding="{Binding Partner.Name}" Width="200"/>
                <DataGridTextColumn Header="Дата" Binding="{Binding CreatedDate, StringFormat=dd.MM.yyyy}" Width="100"/>
                <DataGridTextColumn Header="Сумма" Binding="{Binding TotalAmount, StringFormat=N0}" Width="100"/>
                <DataGridTextColumn Header="Предоплата" Binding="{Binding Prepayment, StringFormat=N0}" Width="100"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding StatusName}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>
        <Border Grid.Row="3" Background="#BBDCFA" CornerRadius="8" Padding="15" Margin="0,15,0,0">
            <StackPanel>
                <TextBlock Text="Товары в заявке" FontWeight="Bold" Margin="0,0,0,10"/>
                <DataGrid x:Name="ItemsGrid" Height="120" AutoGenerateColumns="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Продукция" Binding="{Binding Product.Name}" Width="300"/>
                        <DataGridTextColumn Header="Кол-во" Binding="{Binding Quantity}" Width="80"/>
                        <DataGridTextColumn Header="Цена" Binding="{Binding Price, StringFormat=N0}" Width="100"/>
                        <DataGridTextColumn Header="Дата пр-ва" Binding="{Binding ProductionDate, StringFormat=dd.MM.yyyy}" Width="100"/>
                    </DataGrid.Columns>
                </DataGrid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <Button x:Name="BtnPrepayment" Content="Внести предоплату" Width="140" Height="35" Margin="5,0" Background="#0C4882" Foreground="White" BorderThickness="0" Click="BtnPrepayment_Click"/>
                    <Button x:Name="BtnProduction" Content="Запустить в производство" Width="160" Height="35" Margin="5,0" Background="#0C4882" Foreground="White" BorderThickness="0" Click="BtnProduction_Click"/>
                    <Button x:Name="BtnDelivery" Content="Организовать доставку" Width="160" Height="35" Margin="5,0" Background="#0C4882" Foreground="White" BorderThickness="0" Click="BtnDelivery_Click"/>
                    <Button x:Name="BtnFullPayment" Content="Полная оплата" Width="120" Height="35" Margin="5,0"  Background="#0C4882" Foreground="White" BorderThickness="0" Click="BtnFullPayment_Click"/>
                    <Button x:Name="BtnComplete" Content="Выполнить" Width="100" Height="35" Margin="5,0" Background="#0C4882" Foreground="White" BorderThickness="0" Click="BtnComplete_Click"/>
                    <Button x:Name="BtnCancel" Content="Отменить" Width="100" Height="35" Margin="5,0" Background="#0C4882" Foreground="White" BorderThickness="0" Click="BtnCancel_Click"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Page>