<Page x:Class="Praktika3.WarehouseView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Склад" 
      Background="White"
      FontFamily="Bahnschrift Light SemiCondensed">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Склад" FontSize="22" FontWeight="Bold" Foreground="#0C4882" Margin="0,0,0,20"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <Button x:Name="BtnReceive" Content="Поступление материала" Width="160" Height="35" Margin="10,0" Background="#0C4882" Foreground="White" BorderThickness="0" Click="BtnReceive_Click"/>
            <Button x:Name="BtnWriteOff" Content="Списание материала" Width="160" Height="35" Margin="10,0" Background="#0C4882" Foreground="White" BorderThickness="0" Click="BtnWriteOff_Click"/>
        </StackPanel>
        <TextBlock Grid.Row="2" Text="Материалы на складе" FontSize="16" FontWeight="Bold" Margin="0,0,0,10" Foreground="#0C4882"/>
        <DataGrid x:Name="MaterialsGrid" Grid.Row="3" AutoGenerateColumns="False" IsReadOnly="True" AlternatingRowBackground="#BBDCFA">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Наименование" Binding="{Binding Name}" Width="200"/>
                <DataGridTextColumn Header="Количество" Binding="{Binding QuantityInStock}" Width="100"/>
                <DataGridTextColumn Header="Ед.изм" Binding="{Binding UnitOfMeasure}" Width="80"/>
                <DataGridTextColumn Header="Мин. кол-во" Binding="{Binding MinQuantity}" Width="100"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding StockStatus}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>